---
trigger: model_decision
description: 代码生成、修复、检查、校验以及注释生成等各种批量任务的情况下
---
1. 必须严格按照要求的代码范围全部执行，如果要求的是某个文件夹，就需要对整个文件夹的所有类进行处理；如果要求的是某个项目，就必须对整个项目的所有代码进行处理。
2. 如果遇到代码过多，文件过多，一次性处理不完的情况，先拟定计划表，然后分批次全部自动（不要再问我是否继续执行，默认自动执行），不能遗漏，也不能随机抽取进行处理，我的目的是查漏补缺，你承担的是最后一次校验的责任。