根据这个中文图，创建一个postgresql数据库表的sql语句。要求如下：
1. 将中文属性换成英文字段，并且生成的字段需要对应中文备注，每个创建语句之前，要有判断是否存在该表，存在则删除的语句。
在多张表有对应关系的时候，同样的字段或者描述的同一个字段，对应的单词也要一致。
例如浇水设备表有一个设备号单词对应device_num，那么浇水记录里面的设备号，对应的也应该是device_num，因为我需要建立外键关系，同时不创建外检约束。
2. 表格也需要备注，但是和字段不一样的地方是末尾不要加“表”。例如设备表，应该备注为“设备”。
3. 外键不需要约束，主键统一命名id，varchar 255
4. 表名需要小写，多个单词同下划线隔开
5. 字符串类型默认值为 ""，空的字符串，而不是null，其他类型不需要默认值
一些特殊字段需要固定命名：
    1. 创建时间，统一命名为create_time
    2. 修改时间，统一命名为update_time
    3. 状态，统一命名为status，int2类型
    4. 扩展，统一命名为reserver， text类型
    5. 备注，统一命名为remark
6. 将返回的结果放在一起。但是每个表的sql位置一定要放在一起。例如现在有一个设备表，那么sql大概顺序是：
    1. 设备表的删除语句
    2. 设备表的创建语句
    3. 设备表的备注语句


我需要对 simple-common 项目进行全面的文档完善工作。具体要求如下：

**一、接口注释规范化**
请通读整个项目代码，检查所有接口及其实现类。对于缺失注释或注释不规范的接口方法，请按照标准 Javadoc 格式补全或者修改注释。注释需包含：方法的功能说明、参数说明（@param）、返回值说明（@return）以及可能的异常说明（@throws）。如果原注释中有一些扩展的说明和用法，需要保留，或者在基础上进行规范或者优化，但不能直接删除。例如 ThreadUtils 的类注释里面，就有很多说明，这种需要保留。

**二、项目 README 文档编写**
基于项目代码结构和功能，提炼并输出一份完整的项目介绍与使用说明文档（Markdown 格式）。文档内容需包含：
1. **项目概述**：简述 simple-common 的定位与目标（如：减少重复造轮子、快速开发、新旧项目无侵入集成、性能等方面，也可以给出你对框架的评论）。
2. **模块功能介绍**：逐一介绍各个子模块（如 simple-common-websocket、simple-common-auth、simple-common-logs 等）的核心功能。
3. **核心扩展机制说明**：重点说明在使用各模块时的扩展方式。特别是需要明确指出哪些功能需要通过继承默认实现类（如 DefaultCheckWebSocketManager）或实现特定接口（如 CheckWebSocketManager）来覆盖默认行为，以达到自定义逻辑（如 WebSocket 鉴权、日志用户信息获取等）的目的。
当前存在md文档，但是里面的示例接口调用和实际存在很大偏差，不能用。你可以自己决定是在当前文件修改，还是重新创建一个md文档。

**三、限制条件**
本次任务仅涉及文档编写与注释补充，严禁修改任何接口、参数等代码和逻辑，包括类名或包结构。严禁在文档中输出不存在的接口。请确保输出的 Markdown 文档结构清晰、易于理解。

我需要拜托你做下面几件事情：
1. 全面理解当前代码，重点检测是否有bug，当前逻辑是否合理，业务是否闭环，线程是否安全。
2. 当前模块主要作用是什么？是否解决了问题？给出解决方案的时候，需要确认新给出的方案是不是合理，是不是有bug。更要注意新方案的接入，是否会导致和原代码一起诞生了新的bug。

3. 当前代码设计是否合理，性能如何？如果不合理，或者性能较差，应该如何修改？
4. 移除不必要的、重复的代码和配置
5. 在已有的优化方案中，优先选择高性能，或者对性能损耗低的方案。如果问题在常规使用中几乎遇不到，或者只存在理论上的问题，需要评估是否有必要修复。必要时，需要重新审查修改后的代码，
   防止出现获取修复后的代码后，又有新的bug，浪费时间
6. 所有的修改在原代码的基础上修改，需要完整的给出,方便复制集成。
7. 必须保留方法级别的注释。修复代码完整给出前，必须同步给出为什么这么修改，修改解决了什么问题。

请完成修复，需要满足以下要求：
1. 在已有的优化方案中，优先选择高性能，或者对性能损耗低的方案。如果问题在常规使用中几乎遇不到，或者只存在理论上的问题，需要评估是否有必要修复。必要时，需要重新审查修改后的代码，
   防止出现获取修复后的代码后，又有新的bug，浪费时间
2. 所有的修改在原代码的基础上修改，需要完整的给出,方便复制集成。
3. 必须保留方法级别的注释。每一个修复后的类给出前，必须同步给出为什么这么修改，修改解决了什么问题。