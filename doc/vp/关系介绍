1. 关联（Association）：
    表示两个类之间存在某种联系，但不一定是强依赖关系。
    在图中，关联通常用一条带箭头的实线表示。
2. 继承（Inheritance）：
    表示一个类（子类）继承了另一个类（父类）的属性和方法。
    在图中，继承通常用一条带空心三角箭头的实线表示，箭头指向子类。
3. 实现/实现（Realization/Implementation）：
    表示一个类实现了某个接口或抽象类。
    在图中，实现通常用一条带空心三角箭头的虚线表示，箭头指向实现类。
4. 依赖（Dependency）：
    表示一个类依赖于另一个类的功能。
    在图中，依赖通常用一条带箭头的虚线表示，箭头指向被依赖的类。
5. 聚集（聚合）（Aggregation）：
    表示一种弱的“拥有”关系，即一个类包含另一个类的对象作为它的成员，但成员对象的存在与否并不影响包含它的对象的存在。
    在图中，聚集通常用一条带空心菱形的实线表示，菱形指向包含类。
6. 组合（Composition）：
    表示一种强的“拥有”关系，即一个类包含另一个类的对象作为它的成员，且成员对象与包含它的对象有相同的生命周期。
    在图中，组合通常用一条带实心菱形的实线表示，菱形指向包含类。


例如：
1. a指向b，菱形。往往b方为核心列表，a表示在b列表数据里面，有一个地方可以查询到a的数据。如果b和a关系为多对一，一般表现为按钮查询某某某，点击显示和a数据相关的b的列表。
2. b和a在一对一数据量少的情况下，由后台返回到列表中，例如数据b中包含用户ID，我们需要在的列表中返回用户姓名等基础个别信息。
3. b和a在一对一数据量大的情况下，需要添加按钮，如查看某某详情，点击展示相关数据。