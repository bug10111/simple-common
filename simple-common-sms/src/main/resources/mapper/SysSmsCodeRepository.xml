<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.simple.common.sms.view.sysSmsCode.SysSmsCodeRepository">

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into sys_code_record(id, date, phone, code, ip, req_status, req_results, status, deleted, create_time, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.date}, #{entity.phone}, #{entity.code}, #{entity.ip}, #{entity.reqStatus}, #{entity.reqResults}, #{entity.status}, #{entity.deleted}, #{entity.createTime},
            #{entity.updateTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into sys_code_record(id, date, phone, code, ip, req_status, req_results, status, deleted, create_time, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.date}, #{entity.phone}, #{entity.code}, #{entity.ip}, #{entity.reqStatus}, #{entity.reqResults}, #{entity.status}, #{entity.deleted}, #{entity.createTime},
            #{entity.updateTime})
        </foreach>
        on duplicate key update
        id = values(id),
        date = values(date),
        phone = values(phone),
        code = values(code),
        ip = values(ip),
        req_status = values(req_status),
        req_results = values(req_results),
        status = values(status),
        deleted = values(deleted),
        create_time = values(create_time),
        update_time = values(update_time)
    </insert>

</mapper>

