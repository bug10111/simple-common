<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.simple.oauth.view.sysClientDetails.SysClientDetailsRepository">

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into sys_client_details(id, client_name, client_id, client_secret, resource_ids, scope, access_token_validity, refresh_token_validity, reserve, status, deleted, create_time,
        update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.clientName}, #{entity.clientId}, #{entity.clientSecret}, #{entity.resourceIds}, #{entity.scope}, #{entity.accessTokenValidity}, #{entity.refreshTokenValidity},
            #{entity.reserve}, #{entity.status}, #{entity.deleted}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
    </insert>
</mapper>

