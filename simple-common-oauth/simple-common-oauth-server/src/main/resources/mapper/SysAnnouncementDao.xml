<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.simple.oauth.view.sysAnnouncement.SysAnnouncementRepository">

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into sys_announcement(id, title, data, begin_time, end_time, status, create_time, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.title}, #{entity.data}, #{entity.beginTime}, #{entity.endTime}, #{entity.status}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
    </insert>
</mapper>

