<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.simple.oauth.view.sysUser.SysUserRepository">

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into sys_user(id, username, phone, password, open_id, client_id, is_account_non_expired, is_account_non_locked, is_credentials_non_expired, is_enabled, reserve, deleted, create_time,
        update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.username}, #{entity.phone}, #{entity.password}, #{entity.openId}, #{entity.clientId}, #{entity.isAccountNonExpired}, #{entity.isAccountNonLocked},
            #{entity.isCredentialsNonExpired}, #{entity.isEnabled}, #{entity.reserve}, #{entity.deleted}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
    </insert>

    <select id="findOneByRoleKey" resultType="com.simple.oauth.common.entity.sysUser.SysUser">
        select
        su."id",
        su.username
        from sys_user su
        join sys_user_role sur on sur.user_id=su."id"
        left join sys_role sr on sr."id" = sur.role_id
        where sr.role_key = #{roleKey}
    </select>
    <select id="findOneByRoleId" resultType="com.simple.oauth.common.entity.sysUser.SysUser">
        select
        su."id",
        su.username
        from sys_user su
        join sys_user_role sur on sur.user_id=su."id"
        left join sys_role sr on sr."id" = sur.role_id
        where sr.role_id = #{roleId}
    </select>
</mapper>

