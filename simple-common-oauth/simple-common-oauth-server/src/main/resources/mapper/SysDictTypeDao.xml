<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.simple.oauth.view.sysDictType.SysDictTypeRepository">

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into sys_dict_type(id, dict_name, dict_type, deleted, remark, create_time, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.dictName}, #{entity.dictType}, #{entity.deleted}, #{entity.remark}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
    </insert>
</mapper>

