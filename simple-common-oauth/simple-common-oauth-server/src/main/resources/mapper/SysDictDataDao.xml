<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.simple.oauth.view.sysDictData.SysDictDataRepository">

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into sys_dict_data(id, serial, dict_label, dict_value, dict_type, deleted, remark, create_time, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.serial}, #{entity.dictLabel}, #{entity.dictValue}, #{entity.dictType}, #{entity.deleted}, #{entity.remark}, #{entity.createTime}, #{entity.updateTime})
        </foreach>
    </insert>
</mapper>

